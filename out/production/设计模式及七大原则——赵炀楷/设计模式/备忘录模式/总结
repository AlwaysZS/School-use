备忘录模式：
核心：在不破坏封装性的前提下，捕获一个对象的内部状态，
      并在该对象之外保存这个状态
属于行为型模式

成员：
    Originator:需要保存状态的对象
    Memento：备忘录对象，负责保存好记录
    Caretaker：守护者对象，负责保存多个备忘录对象

相当于SA大法，存档读档
多个对象时可以用hashmap做

适用于各种撤销，恢复，返回，反悔的场景
搭配原型模式可以节约内存

优点：
实现信息的封装，使用户不需要关心保存的细节
缺点：
成员类过多会占用较大资源，并且每一次保存都会消耗一定内存